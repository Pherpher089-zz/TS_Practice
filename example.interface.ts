
// Interfaces work much like types accept that it is exportable. 
// They are better to use than types, so much so that types are
// being depreciated soon.

export interface INumsExample {
    num1: number,
    num2: number,
}